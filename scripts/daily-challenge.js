(()=>{const N=window.NitroArcade=window.NitroArcade||{};N.dailyChallenge={pick(gameIds=[]){if(!gameIds.length)return null;let d=new Date(),key=`${d.getFullYear()}-${d.getMonth()+1}-${d.getDate()}`,seed=[...key].reduce((a,c)=>(a*31+c.charCodeAt(0))>>>0,7);return {date:key,gameId:gameIds[seed%gameIds.length],bonus:50}},complete(gameId){let d=N.dailyChallenge.pick([gameId]);N.save?.set("dailyComplete:"+new Date().toDateString(),true);N.emit?.("dailycomplete",{gameId})}}})();