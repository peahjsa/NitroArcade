(()=>{const N=window.NitroArcade=window.NitroArcade||{};N.economy={balance:()=>N.save?.get("coins",0)||0,add(n,reason=""){let b=Math.max(0,N.economy.balance()+Math.floor(n));N.save?.set("coins",b);N.emit?.("coins",{balance:b,change:n,reason});return b},spend(n,reason=""){n=Math.max(0,Math.floor(n));if(N.economy.balance()<n)return false;N.economy.add(-n,reason);return true},buy(id,cost){if(!N.economy.spend(cost,"buy:"+id))return false;let a=N.save?.get("owned-toys",[])||[];if(!a.includes(id))a.push(id);N.save?.set("owned-toys",a);return true}}})();