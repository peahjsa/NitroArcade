(()=>{const N=window.NitroArcade=window.NitroArcade||{};const norm=s=>String(s||"").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"");N.search={games(list,q){q=norm(q).trim();if(!q)return list;let terms=q.split(/\s+/);return list.map(g=>{let hay=norm([g.name,g.category,g.description,...(g.tags||[])].join(" "));let score=terms.reduce((s,t)=>s+(hay.includes(t)?1:0),0);return {g,score}}).filter(x=>x.score).sort((a,b)=>b.score-a.score).map(x=>x.g)}}})();